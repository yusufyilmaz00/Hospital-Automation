# PostgreSQL bağlantı adresi — kullanıcı adı ve şifre URL içinde OLMAMALI
DB_URL=jdbc:postgresql://<host>/<database>?sslmode=require

# Veritabanı kimlik bilgileri
DB_USERNAME=<kullanici_adi>
DB_PASSWORD=<sifre>
